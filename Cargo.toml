[package]
name = "yaper"
version = "0.1.0"
edition = "2024"

[dependencies]
cpal = "0.15"
clap = { version = "4", features = ["derive"] }
ringbuf = "0.3"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
